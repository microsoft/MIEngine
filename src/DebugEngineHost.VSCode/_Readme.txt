About this project

This project builds the VS Code-compatible version of Microsoft.DebugEngineHost. This dll must export the same surface area as 
the stub version of this dll found in the MIEngine project (MIEngine\src\DebugEngineHost.Stub).
