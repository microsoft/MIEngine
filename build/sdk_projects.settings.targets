<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project=".\package_version.settings.targets" />

  <!-- Don't generate AssemblyInfo.cs with SDK csproj, we need ComVisible and Guid -->
  <PropertyGroup>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>
</Project>